# GenAI provider configuration (free-tier oriented default)
GENAI_PROVIDER=gemini
GENAI_MODEL=gemini-3.5-flash-lite

# Configure locally; never commit a real key.
GOOGLE_API_KEY=replace-with-local-api-key
